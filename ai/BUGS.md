# DANH SÁCH LỖI (BUGS TRACKER)

File này dùng để theo dõi các lỗi phát sinh trong quá trình phát triển (đặc biệt hữu ích khi làm việc nhóm và nhờ AI fix lỗi).
Yêu cầu ghi lỗi: Tóm tắt ngắn gọn nhưng PHẢI ghi cụ thể nguyên nhân/file/dòng bị lỗi.

| Trạng Thái       | Ngày phát hiện | Mô tả lỗi ngắn gọn (Chỉ rõ File/Class/Line)                                                                                                                                                                                                                                                                                                                                                                                                                            | Người fix / AI fix | Ghi chú / Cách giải quyết                                                                                                                                                                                                                                                       |
| :--------------- | :------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `[x]` (Resolved) | 2026-09-22     | **Lỗi UI/UX cuộn trang (Scroll Restoration)**: Khi người dùng cuộn xuống dưới ở một trang rồi chuyển sang trang/route mới, màn hình không tự động cuộn lên đầu trang mà vẫn giữ nguyên vị trí cuộn cũ (`src/routes/AppRoutes.jsx`). _(Phát hiện bởi: **Thành Đạt**)_                                                                                                                                                                                                   | AI (Antigravity)   | Tạo component `ScrollToTop.jsx` sử dụng `useLocation()` và hook `useEffect(() => window.scrollTo({ top: 0, left: 0, behavior: 'instant' }), [pathname])`, sau đó mount trực tiếp vào `<BrowserRouter>` trong `AppRoutes.jsx`. Đã kiểm tra build thành công.                     |
| `[x]` (Resolved) | 2026-09-23     | **Lỗi JSX Syntax Parser (Vite oxc)**: Lỗi cú pháp thẻ JSX bị vỡ cấu trúc tại `frontend/src/pages/public/SitemapPage.jsx` (dòng 97-105). Thẻ `<div>` ở dòng 97-98 chưa được đóng hợp lệ và xuất hiện thẻ đóng `</li>`, `</ul>` mồ côi từ template cũ, dẫn đến lỗi build _"[plugin:vite:oxc] Transform failed with 3 errors: Expected corresponding JSX closing tag for 'div' & Adjacent JSX elements must be wrapped in an enclosing tag"_. _(Phát hiện bởi: **User**)_ | AI (Antigravity)   | Tái cấu trúc và hoàn thiện chuẩn xác 3 khối điều hướng của MarketLink Sitemap (`SitemapPage.jsx`): Public Marketplace, Role-Based Portals (Shopper, Operator, Admin) và Auth/Security; đóng đầy đủ và hợp lệ các thẻ JSX. Kiểm tra bằng `oxlint` và `npm run build` thành công. |
| `[x]` (Resolved) | 2026-09-23     | **Lỗi UI/UX Navbar bị xuống dòng trên Laptop (125%/150% Scaling) & Thiếu Responsive Mobile Drawer**: Các link điều hướng văn bản dài như "Markets Directory", "Fresh Produce", "About Us", "Harvest Gallery" bị ngắt thành 2 dòng làm vỡ thanh Header; đồng thời ở màn hình hẹp (< 1024px) thanh điều hướng bị ẩn hoàn toàn mà không có nút mở menu di động (`frontend/src/components/layout/PublicNavbar.jsx`). _(Phát hiện bởi: **User**)_ | AI (Antigravity)   | Rút gọn nhãn điều hướng Desktop thành 1 từ duy nhất: `Home`, `Markets`, `Produce`, `About`, `Gallery`, `Feedback`, `Contact`, `Sitemap` kết hợp `whitespace-nowrap`; bổ sung nút Hamburger toggle (`Menu`/`X`) và Drawer menu trượt xuống cho Mobile/Tablet với tính năng tự động đóng khi chuyển route. |
| `[x]` (Resolved) | 2026-09-23     | **Lỗi Legacy Emergency Ambulance Content trong ContactPage**: Trang liên hệ `frontend/src/pages/public/ContactPage.jsx` chưa được cập nhật theo đề bài TechWiz 7 (MarketLink eGreen Basket), vẫn còn chứa 100% nội dung y tế cứu thương ("24/7 Dispatch", "Paramedic Fleet", "Hospital HQ"). _(Phát hiện bởi: **User**)_ | AI (Antigravity)   | Viết lại hoàn toàn `ContactPage.jsx` chuẩn chủ đề MarketLink: Chicago Green Loop Market Operations HQ, hotline `(312) 555-FARM`, email `support@marketlink.org`, form gửi yêu cầu có phân loại chủ đề (Pre-order, Farmer application, Market visit, v.v.), chỉ dẫn chợ và bản đồ trực quan. |
| `[x]` (Resolved) | 2026-09-23     | **Lỗi Modal Form "Add New Farmers Market" Bị Trắng & Sai Luồng Điều Hướng Admin / Thiếu Mobile Drawer**: (1) Trong `AdminDashboard.jsx`, hàm `showCustomModal` truyền prop `render` thay vì `content` khiến form thêm chợ không hiển thị; (2) Trong `AdminLayout.jsx`, các link sidebar như "Customer Reviews", "Contact Messages", "Platform Sitemap" lại dẫn ra trang public của user thay vì các module quản trị; (3) Cả `AdminLayout` và `OperatorLayout` đều ẩn sidebar trên mobile (`< 1024px`) mà không có nút Hamburger/Drawer để điều hướng. _(Phát hiện bởi: **User**)_ | AI (Antigravity)   | (1) Cập nhật `ModalContext.jsx` hỗ trợ cả `content` và `render` kèm dual callback signature; sửa hàm gọi trong `AdminDashboard.jsx` để form thêm chợ hiển thị đầy đủ; (2) Tái cấu trúc sidebar của `AdminLayout.jsx` đồng bộ với các tab quản trị nội bộ (`?tab=markets`, `?tab=vendors`, `?tab=users`, `?tab=reviews`, `?tab=messages`, `?tab=reports`), chuyển link public xuống mục riêng; (3) Trang bị nút Hamburger và Mobile Drawer trượt cho cả `AdminLayout` và `OperatorLayout`; (4) Bổ sung 2 tab thiếu theo SRS: **Quản lý Tài khoản (User Accounts)** và **Báo cáo Phân tích (Reports & Analytics)** kèm tính năng Edit/Delete chợ. |
| `[x]` (Resolved) | 2026-09-23     | **Lỗi Chart.js Controller Chưa Đăng Ký & Canvas Already In Use**: Tại component `frontend/src/pages/admin/AdminAnalyticsCharts.jsx`, trình duyệt báo lỗi `Uncaught Error: "line" is not a registered controller` và `Uncaught Error: Canvas is already in use. Chart with ID '0' must be destroyed before the canvas with ID '' can be reused`. Nguyên nhân: (1) Chart.js v4 tree-shaking đăng ký thiếu `LineController` và `DoughnutController`; (2) Khi constructor bị lỗi giữa chừng hoặc React StrictMode/Hot-Reload kích hoạt lại `useEffect`, instance cũ chưa bị hủy triệt để khỏi registry của Chart.js bằng `Chart.getChart(canvas)?.destroy()`. _(Phát hiện bởi: **User**)_ | AI (Antigravity)   | (1) Đổi sang import gói tích hợp tự động đầy đủ `import Chart from 'chart.js/auto'` để tự động nạp tất cả controller (`line`, `doughnut`, `bar`, v.v.), scales và elements; (2) Trước khi tạo chart mới và trong hàm cleanup của `useEffect`, gọi `Chart.getChart(canvasRef.current)?.destroy()` để dọn sạch canvas an toàn 100% trong mọi kịch bản re-render/hot-reload. |
| `[ ]` (Open)     |                |                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |                    |                                                                                                                                                                                                                                                                                 |

